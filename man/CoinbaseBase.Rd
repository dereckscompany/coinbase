% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CoinbaseBase.R
\name{CoinbaseBase}
\alias{CoinbaseBase}
\title{CoinbaseBase: Abstract Base Class for Coinbase API Clients}
\description{
CoinbaseBase: Abstract Base Class for Coinbase API Clients

CoinbaseBase: Abstract Base Class for Coinbase API Clients
}
\details{
Provides shared infrastructure for all Coinbase R6 classes by extending
\link[connectcore:RestClient]{connectcore::RestClient}. It inherits the single \code{private$.request()} funnel
(mode-transparent sync/async, NULL-field stripping, retry/throttle) and
customises only the two venue-specific seams:
\itemize{
\item \code{.sign()} — attaches a Coinbase JWT (ES256 / EdDSA) to each authenticated
request (via the internal \code{coinbase_jwt_sign()}).
\item \code{.parse_envelope()} — reads the Coinbase error envelope and tolerates the
empty success bodies some endpoints return (via the internal
\code{parse_coinbase_response()}).
}
\subsection{Sync vs Async}{

The \code{async} parameter controls execution mode for all API methods:
\itemize{
\item \code{async = FALSE} (default): methods return results directly.
\item \code{async = TRUE}: methods return \link[promises:promise]{promises::promise} objects that resolve to
the same types.
}

Async mode requires the \code{promises} package (a \code{Suggests}). Consume promises
with \code{\link[coro:async]{coro::async()}} and \code{await()} or \code{\link[promises:then]{promises::then()}}; to drive the event
loop in a script use the (optional) \code{later} package, e.g.
\code{while (!later::loop_empty()) later::run_now()}.
}

\subsection{Hosts}{

Coinbase splits across two hosts. Authenticated trading and account endpoints
live on the Advanced Trade host (\code{\link[=get_base_url]{get_base_url()}},
\verb{https://api.coinbase.com}); the public market-data endpoints with deep
history live on the Exchange host (\code{\link[=get_exchange_base_url]{get_exchange_base_url()}},
\verb{https://api.exchange.coinbase.com}). Subclasses select the host per request
via the \code{base_url} argument of \code{private$.request()} (this class extends the
connectcore funnel with that argument).
}

\subsection{Design}{

This class is not meant to be instantiated directly. Subclasses (e.g.
\code{CoinbaseMarketData}, \code{CoinbaseTrading}) inherit from it and define public
methods that delegate to \code{private$.request()}.
}
}
\section{Fields}{

All fields are private:
\itemize{
\item \code{.exchange_base_url}: Character; Exchange API base URL (the Advanced Trade
base, credentials, async flag, and perform function are held by the
\link[connectcore:RestClient]{connectcore::RestClient} superclass).
}
}

\section{Super class}{
\code{\link[connectcore:RestClient]{connectcore::RestClient}} -> \code{CoinbaseBase}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-CoinbaseBase-new}{\code{CoinbaseBase$new()}}
\item \href{#method-CoinbaseBase-clone}{\code{CoinbaseBase$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-CoinbaseBase-new"></a>}}
\if{latex}{\out{\hypertarget{method-CoinbaseBase-new}{}}}
\subsection{Method \code{new()}}{
Initialise a CoinbaseBase object.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{CoinbaseBase$new(
  keys = get_api_keys(),
  base_url = get_base_url(),
  exchange_base_url = get_exchange_base_url(),
  async = FALSE
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{keys}}{(list | NULL) API credentials from \code{\link[=get_api_keys]{get_api_keys()}}. Defaults
to \code{get_api_keys()}.}

\item{\code{base_url}}{(scalar<character>) Advanced Trade API base URL. Defaults
to \code{get_base_url()}.}

\item{\code{exchange_base_url}}{(scalar<character>) Exchange API base URL.
Defaults to \code{get_exchange_base_url()}.}

\item{\code{async}}{(scalar<logical>) if \code{TRUE}, methods return promises. Default
\code{FALSE}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(class<CoinbaseBase>) invisibly, self.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-CoinbaseBase-clone"></a>}}
\if{latex}{\out{\hypertarget{method-CoinbaseBase-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{CoinbaseBase$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}

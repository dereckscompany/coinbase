% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/types_coinbase.R
\name{coinbase_shapes}
\alias{coinbase_shapes}
\title{Coinbase return shapes}
\description{
Reusable roxyassert \verb{@type} shapes for the parsed Coinbase
\code{data.table}s. Every column is typed to what the parser actually produces:
\code{numeric} is the strict double (the package convention); a column is marked
\verb{| NA} wherever the parser can emit a missing value -- which is most of them,
because the parsers coalesce absent JSON fields to \code{NA} (\code{num_or_na},
\code{coalesce_null(.., NA_character_)}, \code{iso_to_datetime} on a missing string) and
the field may legitimately be absent in a real response. So that an empty
result still satisfies its contract, each parser's empty branch returns the
fully-typed zero-row table for its shape (see \verb{empty_dt_*()} in
R/helpers_parse.R) rather than a bare schemaless \code{data.table()}.

Shapes: \code{Products}, \code{Ohlcv}, \code{Trades}, \code{Stats}, \code{ProductStats}, \code{BestBidAsk},
\code{Accounts}, \code{Fees}, \code{OrderConfig} (the flattened \code{order_configuration}
record), \code{Orders}, \code{Fills}, \code{Preview}, \code{CreateOrderAck}, \code{EditOrderAck},
\code{EditPreview}, \code{CancelResults}, \code{MarginWindow}, \code{FuturesBalance},
\code{FuturesPositions}, \code{FuturesSweeps}, \code{PortfolioSummary}. The order book
(\code{parse_orderbook}) and the portfolio breakdown (\code{parse_portfolio_breakdown})
return level/source-dependent column sets and are documented inline at those
parsers.

Each shape is referenced by a table-returning method's \verb{@return} as
\verb{(Shape | promise<Shape>)}, so the contract roclet expands it inline into that
method's generated \verb{assert_return_*} -- no standalone \verb{assert_type_<Shape>()}
is emitted. coinbase is a leaf connector: nothing internal calls a per-shape
validator and no downstream package validates against these shapes, so there
is no \verb{@genassert} (no callable validators to generate) and no
\verb{@exportassert} (nothing to export).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_request.R
\name{coinbase_build_request}
\alias{coinbase_build_request}
\title{Build and Execute a Coinbase API Request}
\usage{
coinbase_build_request(
  base_url,
  endpoint,
  method = "GET",
  query = list(),
  body = NULL,
  keys = NULL,
  .perform = httr2::req_perform,
  .parser = identity,
  is_async = FALSE,
  timeout = 30
)
}
\arguments{
\item{base_url}{Character; the API base URL (scheme + host).}

\item{endpoint}{Character; the API path.}

\item{method}{Character; HTTP method. Default \code{"GET"}.}

\item{query}{Named list; query parameters. Default \code{list()}.}

\item{body}{Named list or NULL; request body. Default \code{NULL}.}

\item{keys}{List or NULL; API credentials. When non-NULL the request is
signed. Default \code{NULL}.}

\item{.perform}{Function; the httr2 perform function. Default \code{httr2::req_perform}.}

\item{.parser}{Function; post-processing applied to the parsed response body.
Default \code{identity}.}

\item{is_async}{Logical; whether \code{.perform} returns promises. Default \code{FALSE}.}

\item{timeout}{Numeric; request timeout in seconds. Default \code{30}.}
}
\value{
Parsed and post-processed API response data, or a promise thereof.
}
\description{
Constructs an \link[httr2:request]{httr2::request}, optionally attaches a signed JWT, performs it
via the supplied \code{.perform} function, and parses the JSON response. This is
the single point through which all Coinbase API calls flow.
}
\details{
\subsection{Sync vs Async}{

The \code{.perform} argument controls execution mode:
\itemize{
\item \code{httr2::req_perform} (default): synchronous, returns an \link[httr2:response]{httr2::response}.
\item \code{httr2::req_perform_promise}: asynchronous, returns a \link[promises:promise]{promises::promise}.
}
}
}

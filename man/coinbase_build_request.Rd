% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_request.R
\name{coinbase_build_request}
\alias{coinbase_build_request}
\title{Build and Execute a Coinbase API Request}
\usage{
coinbase_build_request(
  base_url,
  endpoint,
  method = "GET",
  query = list(),
  body = NULL,
  keys = NULL,
  .perform = httr2::req_perform,
  .parser = identity,
  is_async = FALSE,
  timeout = 30
)
}
\arguments{
\item{base_url}{(scalar<character>) the API base URL (scheme + host).}

\item{endpoint}{(scalar<character>) the API path.}

\item{method}{(scalar<character>) HTTP method. Default \code{"GET"}.}

\item{query}{(list) query parameters. Default \code{list()}.}

\item{body}{(list | NULL) request body. Default \code{NULL}.}

\item{keys}{(list | NULL) API credentials. When non-NULL the request is
signed. Default \code{NULL}.}

\item{.perform}{(function) the httr2 perform function. Default
\code{httr2::req_perform}.}

\item{.parser}{(function) post-processing applied to the parsed response body.
Default \code{identity}.}

\item{is_async}{(scalar<logical>) whether \code{.perform} returns promises. Default
\code{FALSE}.}

\item{timeout}{(scalar<numeric in ]0, Inf[>) request timeout in seconds.
Default \code{30}.}
}
\value{
(any | promise<any>) parsed and post-processed API response data, or a
promise thereof.
}
\description{
Constructs an \link[httr2:request]{httr2::request}, optionally JWT-signs it, performs it (sync or
async), and parses the Coinbase response envelope. This is the single point
through which all Coinbase API calls flow; it is a thin Coinbase-specific
wrapper over \code{\link[connectcore:build_request]{connectcore::build_request()}} that injects the two seams that
differ per venue — JWT signing (the internal \code{coinbase_jwt_sign()}) and the
Coinbase error/empty-body envelope (the internal \code{parse_coinbase_response()}).
Everything else (the
sync/async branch, NULL-field stripping, the JSON body, retry, throttle) comes
from connectcore.
}
\details{
\subsection{Sync vs Async}{

The \code{.perform} argument controls execution mode:
\itemize{
\item \code{httr2::req_perform} (default): synchronous, returns the parsed data.
\item \code{httr2::req_perform_promise}: asynchronous, returns a \link[promises:promise]{promises::promise}.
}
}
}

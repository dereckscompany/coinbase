% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{get_api_keys}
\alias{get_api_keys}
\title{Retrieve Coinbase API Credentials}
\usage{
get_api_keys(
  api_key_name = Sys.getenv("COINBASE_API_KEY_NAME"),
  api_private_key = Sys.getenv("COINBASE_API_PRIVATE_KEY")
)
}
\arguments{
\item{api_key_name}{Character string; the credential \code{name}, e.g.
\code{"organizations/<org-uuid>/apiKeys/<key-uuid>"}. Defaults to
\code{Sys.getenv("COINBASE_API_KEY_NAME")}.}

\item{api_private_key}{Character string; the credential \code{privateKey}.
Defaults to \code{Sys.getenv("COINBASE_API_PRIVATE_KEY")}.}
}
\value{
Named list with \code{api_key_name} and \code{api_private_key} (newlines
unescaped).
}
\description{
Fetches Coinbase Advanced Trade (CDP) API credentials from environment
variables or explicit arguments. Credentials are created at
\url{https://www.coinbase.com/settings/api} and downloaded as a JSON file
containing a \code{name} and a \code{privateKey}.
}
\details{
Required environment variables: \code{COINBASE_API_KEY_NAME},
\code{COINBASE_API_PRIVATE_KEY}.
\subsection{Private key formatting}{

The downloaded \code{privateKey} is a multi-line value. To store it on a single
line in \code{.Renviron}, escape the newlines as the two characters \verb{\\\\n}; this
function unescapes them back to real newlines before use. Both EC
(\verb{-----BEGIN EC PRIVATE KEY-----}, signed with ES256) and base64-encoded
Ed25519 keys (signed with EdDSA) are supported by the signer.
}
}
\examples{
\dontrun{
keys <- get_api_keys()
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backfill.R
\name{coinbase_backfill_trades}
\alias{coinbase_backfill_trades}
\title{Backfill Coinbase Trade History to CSV}
\usage{
coinbase_backfill_trades(
  symbols,
  from = lubridate::now("UTC") - lubridate::dweeks(1),
  to = lubridate::now("UTC"),
  file,
  base_url = get_base_url(),
  exchange_base_url = get_exchange_base_url(),
  max_pages = Inf,
  sleep = 0.3,
  verbose = TRUE
)
}
\arguments{
\item{symbols}{Character vector of product symbols (e.g.
\code{c("BTC-USD", "ETH-USD")}). Must not be NULL or empty.}

\item{from}{POSIXct or numeric; start of the backfill window. Defaults to one
week ago. Tick volume is large, so widen this deliberately.}

\item{to}{POSIXct or numeric; end of the window. Defaults to the current time.}

\item{file}{Character; path to the output CSV. Data is appended incrementally
so progress survives interruption.}

\item{base_url}{Character; Advanced Trade API base URL.}

\item{exchange_base_url}{Character; Exchange API base URL.}

\item{max_pages}{Numeric; per-symbol cap on pages fetched. Default \code{Inf}.}

\item{sleep}{Numeric; seconds to sleep between symbols to respect rate limits.}

\item{verbose}{Logical; if \code{TRUE}, prints progress via \code{\link[rlang:inform]{rlang::inform()}}.}
}
\value{
The file path (invisibly). If any symbols failed, a \code{"failures"}
attribute is attached: a \link[data.table:data.table]{data.table::data.table} with columns \code{symbol}
and \code{error}.
}
\description{
Downloads deep tick history for one or more products and writes the results
incrementally to a CSV file. Supports resuming a partially completed backfill
by reading the existing file and continuing each product from its last
recorded trade. This is the foundation of the data-collection pipeline:
aggregate the resulting ticks with \code{\link[=trades_to_ohlcv]{trades_to_ohlcv()}} for OHLCV at any
timeframe.
}
\details{
Paging always begins at the most recent trade and walks backwards toward
\code{from}; for a fresh (non-resumed) run, leaving \code{from} as the default pulls a
bounded recent window rather than the product's entire history.
}
\examples{
\dontrun{
coinbase_backfill_trades(
  symbols = c("BTC-USD", "ETH-USD"),
  from = lubridate::as_datetime("2026-05-01"),
  file = "trades.csv"
)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backfill.R
\name{coinbase_backfill_klines}
\alias{coinbase_backfill_klines}
\title{Backfill Coinbase OHLCV Candles to CSV}
\usage{
coinbase_backfill_klines(
  symbols,
  timeframes = "1day",
  from = lubridate::now("UTC") - lubridate::ddays(365),
  to = lubridate::now("UTC"),
  file,
  base_url = get_base_url(),
  exchange_base_url = get_exchange_base_url(),
  max_bars = 300L,
  sleep = 0.3,
  verbose = TRUE
)
}
\arguments{
\item{symbols}{(character) product symbols (e.g. \code{c("BTC-USD", "ETH-USD")}).
Must not be NULL or empty.}

\item{timeframes}{(character) candle timeframes; the Coinbase granularities
\code{"1min"}, \code{"5min"}, \code{"15min"}, \code{"1hour"}, \code{"6hour"}, \code{"1day"}. Default
\code{"1day"}.}

\item{from}{(POSIXct | numeric) start of the backfill window. Defaults to one
year ago.}

\item{to}{(POSIXct | numeric) end of the backfill window. Defaults to the
current time.}

\item{file}{(scalar<character>) path to the output CSV. Data is appended
incrementally so progress survives interruption.}

\item{base_url}{(scalar<character>) Advanced Trade API base URL.}

\item{exchange_base_url}{(scalar<character>) Exchange API base URL (the host
that serves \verb{/candles}).}

\item{max_bars}{(scalar<count in [1, Inf[>) candles per request window (the
endpoint cap). Default 300.}

\item{sleep}{(scalar<numeric in [0, Inf[>) seconds to sleep between request
windows and between combinations to respect rate limits.}

\item{verbose}{(scalar<logical>) if \code{TRUE}, prints progress via
\code{\link[rlang:abort]{rlang::inform()}}.}
}
\value{
(scalar<character>) the file path (invisibly). The written CSV has
columns \code{datetime}, \code{open}, \code{high}, \code{low}, \code{close}, \code{volume}, \code{symbol},
\code{timeframe}.
}
\description{
Downloads historical price candles (open/high/low/close/volume) for one or
more products across one or more timeframes and writes them to a CSV file, one
row per candle. Coinbase only returns about 300 candles per request, so this
walks the whole date range for you, page by page, and appends as it goes.
Re-running it against the same file resumes from the last saved candle, so an
interrupted run is safe to just restart. This is the Coinbase sibling of
\code{\link[=coinbase_backfill_trades]{coinbase_backfill_trades()}} and mirrors the kucoin/binance
\verb{*_backfill_klines()} contract.
}
\details{
\strong{Window mechanics.} The Coinbase Exchange \verb{/candles} endpoint caps a single
response at ~300 bars, so each \verb{(symbol, timeframe)} range is tiled into
contiguous windows of at most \code{max_bars} bars driven over
\code{CoinbaseMarketData$get_ohlcv()}. Windows are inclusive of both ends and the
next starts one step past the previous end, so they tile the range without a
boundary gap. Epoch-second arithmetic is done in double, never 32-bit integer,
so day-granularity spans past 2038 cannot silently overflow.

\strong{Resume semantics.} When \code{file} already holds rows for a \verb{(symbol, timeframe)}, the fetch restarts \emph{inclusively} from that combination's last
stored candle and the boundary duplicate is collapsed — only strictly-newer
candles are appended — so a restart can neither strand a gap nor repeat a row.
A combination whose last stored candle already reaches \code{to} is skipped as up to
date.

\strong{Dedup and closed candles.} Pages are unioned and deduplicated by
\code{datetime}, sorted ascending. Only \emph{closed} candles are written: the bar still
forming at the live edge (open time \code{t} where \code{t + step > now}) is dropped so a
half-built candle is never persisted (a resume would otherwise advance past it
and never refresh it).

\strong{Failures.} Each failed \verb{(symbol, timeframe)} combination raises one
\code{\link[rlang:abort]{rlang::warn()}}, and after the loop a final summary warning lists the count and
the affected combinations (the kucoin/binance convention downstream failure
ledgers key on). No failure data is hidden on the return value.
}
\examples{
\dontrun{
coinbase_backfill_klines(
  symbols = c("BTC-USD", "ETH-USD"),
  timeframes = c("1day", "1hour"),
  from = lubridate::as_datetime("2020-01-01"),
  file = "coinbase_klines.csv"
)
}
}
